Use runbm.sh (Run benchmark script) to run the benchmarks

Download DaCapo benchmark jar from the URL given below and place it in 
testprogs/DaCapo directory
<URL>

This DaCapo jar (dacapo-9.12-bach.jar) has a custom MyCounter class. You might 
run into errors if you use the any other jars available online. 
So, use the one provided instead.